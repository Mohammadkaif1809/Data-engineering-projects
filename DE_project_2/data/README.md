
The datasets used in this project are retrieved from the DummyJSON REST API
and transformed using Python and Pandas.

The generated CSV files are not stored in this repository.
They can be regenerated by running the API ingestion and transformation notebook.